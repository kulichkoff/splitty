services:
  db:
    image: postgres:17-alpine
    restart: always
    env_file:
      - secrets.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
